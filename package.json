{
  "name": "armor-rpg-forest-mvp",
  "version": "2.0.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "dev": "node scripts/dev-server.js",
    "build": "npm run check",
    "lint": "npm run check",
    "test": "npm run smoke",
    "smoke": "node scripts/smoke-test.js",
    "check": "node --check app.js && node --check api/app.js && node --check server/api.js && node --check server/core.js && node --check server/store.js && node --check scripts/dev-server.js && node --check scripts/smoke-test.js"
  },
  "dependencies": {
    "pg": "^8.22.0"
  }
}
