{
  "name": "armor-qr-rpg-delivery-extra",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "node scripts/build-package.js && node scripts/generate-pdfs.js",
    "verify": "node scripts/verify-package.js",
    "check": "npm run build && npm run verify"
  },
  "dependencies": {
    "pdfkit": "^0.19.1",
    "qrcode": "^1.5.4"
  }
}
